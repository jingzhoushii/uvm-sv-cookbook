# 01-data-types - SystemVerilog 数据类型

## 📚 知识点

- **四值逻辑** vs **二值逻辑**
- **有符号** vs **无符号**
- **定宽数组** vs **动态数组**
- **队列** 和 **关联数组**
- **结构体** 和 **枚举**
- **类型转换** 和 **位宽扩展**

## 📖 背景知识

### 四值逻辑 vs 二值逻辑

| 类型 | 取值 | 存储 | 典型使用 |
|------|------|------|----------|
| **四值逻辑** | 0, 1, X, Z | 4 位 | logic, wire, reg |
| **二值逻辑** | 0, 1 | 2 位 | bit, int, longint |

```systemverilog
logic   [7:0] data;  // 四值逻辑：可为 X/Z
bit     [7:0] count; // 二值逻辑：0 或 1
```

### 有符号 vs 无符号

| 类型 | 符号 | 范围 |
|------|------|------|
| `int` | 有符号 | -2^31 ~ 2^31-1 |
| `unsigned int` | 无符号 | 0 ~ 2^32-1 |
| `byte` | 有符号 | -128 ~ 127 |
| `bit [7:0]` | 无符号 | 0 ~ 255 |

## 📂 文件结构

```
01-data-types/
├── README.md              # 本文档
├── Makefile              # 编译脚本
├── filelist.f            # 文件列表
├── dut/
│   └── simple_alu.sv     # ALU 被测设计
├── tb/
│   └── tb_data_types.sv  # 测试平台
└── examples/
    ├── 01_basic_types.sv   # 基础类型
    ├── 02_arrays.sv        # 数组
    ├── 03_struct_enum.sv   # 结构体/枚举
    └── 04_type_casting.sv  # 类型转换
```

## 🔍 代码导读

### 基础类型示例

```systemverilog
// 四值逻辑
logic [31:0] addr;     // 可为 X/Z，用于总线
reg    [7:0]  data;    // 经典 Verilog 类型

// 二值逻辑
bit   [31:0] counter;  // 用于纯逻辑计数
int   count;           // 32 位有符号整数
longint large;         // 64 位有符号

// 定宽数组
bit [7:0] mem[0:255];  // 256 字节存储
int  matrix[4][4];     // 4x4 二维数组

// 动态数组
int dyn[];             // 运行时确定大小
dyn = new[16];         // 分配 16 个元素

// 队列
int queue[$];          // 自动扩展队列
queue.push_back(5);    // 添加元素
int q = queue.pop_front(); // 取出

// 关联数组
int assoc[int];        // 索引任意
assoc[10] = 100;       // 稀疏存储
```

## 🚀 快速开始

```bash
cd 01-sv-fundamentals/01-data-types

# VCS (默认)
make

# Xcelium
make SIM=xrun

# Questa
make SIM=vsim

# 清理
make clean
```

## 💡 示例说明

### 01_basic_types.sv
演示所有基础数据类型的定义和赋值

### 02_arrays.sv
演示定宽数组、动态数组、队列、关联数组的创建和操作

### 03_struct_enum.sv
演示结构体打包（packed）和不打包（unpacked）区别

### 04_type_casting.sv
演示静态转换 `$cast()` 和动态转换

## 📝 练习题

1. **练习 1**：创建一个 2D 动态数组，实现矩阵乘法
2. **练习 2**：使用关联数组实现 LRU 缓存
3. **练习 3**：使用队列实现 FIFO

## ⚠️ 常见问题

### Q1: `logic` vs `reg`/`wire` 区别？

**A**: `logic` 可自动推断为 reg 或 wire，比传统类型更智能

### Q2: 为什么要用二值逻辑？

**A**: 仿真更快，RTL 和综合结果更一致

### Q3: `$cast` 什么时候用？

**A**: 父类向子类转换时，或枚举类型转换时

## 📚 参考资料

- [IEEE 1800-2017 SystemVerilog LRM](https://ieeexplore.ieee.org/document/1800799)
- [ChipVerify - Data Types](https://www.chipverify.com/systemverilog/systemverilog-data-types)
- [Verification Academy - Data Types](https://verificationacademy.com/cookbook/sv/datatype)

---

## 👤 作者

**GitHub**: [@jingzhoushii](https://github.com/jingzhoushii)

---

**快速导航**: [返回根目录](../../README.md) | [上一章节](../README) | [下一章节](../02-procedural-blocks)
