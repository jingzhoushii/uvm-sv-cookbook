# 02-uvm_env - UVM 环境

## 📚 知识点

- **uvm_env** 验证环境容器
- **组件组装**
- **TLM 连接**
- **配置传递**

## 📖 背景知识

UVM Environment 是验证平台的最顶层容器，用于组装所有验证组件。

## 📂 文件结构

```
02-uvm_env/
├── examples/
│   └── mem_ctrl/         # 完整内存控制器示例 ✅
│       ├── Makefile     # 编译脚本
│       ├── mem_if.sv    # 接口
│       ├── mem_dut.sv   # DUT
│       ├── mem_transaction.sv  # 事务
│       ├── mem_config.sv       # 配置
│       ├── mem_driver.sv        # 驱动
│       ├── mem_monitor.sv       # 监控
│       ├── mem_env.sv           # 环境
│       ├── mem_seq.sv           # 序列
│       └── tb_mem.sv            # 测试平台
└── tests/
    └── mem_test.sv      # 测试用例
```

## 🚀 快速开始

```bash
cd 03-uvm-components/02-uvm_env/examples
make SIM=vcs
```

---

**快速导航**: [返回根目录](../../README.md) | [上一章节](../01-uvm_component) | [下一章节](../03-uvm_agent)
